# String Casing

## capitalize

Capitalizes the first grapheme of a string.

```ts
capitalize(input?: string, locale?: string): string
```

- Returns an empty string for null or undefined input
- Respects locale-specific casing rules

## capitalizeWords

Capitalizes the first grapheme of each word.

## lower / upper

Locale-aware lowercase and uppercase conversions.
